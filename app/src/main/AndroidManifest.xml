<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.example.hatewait">

    <uses-permission android:name="android.permission.INTERNET " />
    <uses-permission android:name="android.permission.VIBRATE " />
    <uses-permission android:name="android.permission.WAKE_LOCK " />

    <application
            android:allowBackup="true"
            android:icon="@drawable/app_logo"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".StoreNameChangeDialog"></activity>
        <activity android:name=".StoreInfoUpdate">
        </activity>
        <activity android:name=".CustomerMenu" />
         <activity android:name=".StoreMenu"/>
        <activity android:name=".LoginRegisterViewPagerActivity" />
        <activity android:name=".Register_Check" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Add_waiting" />
        <activity android:name=".CustomerWaitingList" />
         <activity android:name=".StoreWaitingList" />
        <activity android:name=".BusinessHourPick" />
        <activity
                android:name=".Splash"
                android:screenOrientation="portrait"
                android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
                tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>